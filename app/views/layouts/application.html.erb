<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Rally results online">
  <meta name="author" content="Giedrius Rimkus, Giedrius Šileikis, Darius Šileikis">
  <title>raceonline.lt - lenktynių rezultatai gyvai</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>

  <!--[if lt IE 9]>
  <script src="/assets/js/html5shiv.js"></script>
  <![endif]-->

  <style type="text/css">
  body {
    padding-top: 0px;
    padding-bottom: 10px;
  }

  /* Custom container */
  .container-narrow {
    margin: 0 auto;
    max-width: 700px;
  }
  .container-narrow > hr {
    margin: 30px 0;
  }

  /* Supporting marketing content */
  .marketing {
    margin: 60px 0;
  }
  .marketing p + h4 {
    margin-top: 28px;
  }
  </style>
</head>
<body>

  <div class="container-narrow">
    <div class="masthead">
      <h3 class="muted">raceonline.lt</h3>
      <hr>
      <%= render 'layouts/navigation' %>
    </div>

    <div class="row-fluid marketing">
      <% if user_signed_in? %>
        <div class="alert"><%= current_user.email %> | <%= link_to 'Atsijungti', sign_out_path, :method => :destroy %></div>
      <% end %>
      <%= render 'layouts/messages' %>
      <%= yield %>
    </div>

    <hr>
    <div class="footer">
      raceonline.lt &copy; 2013
    </div>
  </div>

</body>
</html>
